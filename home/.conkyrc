conky.config = {
    alignment = 'top_right',
    background = true,
    border_width = 0,
    double_buffer = true,
    default_color = '#e24358',
    color1 = '#9cb9de',
    draw_borders = false,
    draw_outline = false,
    draw_shades = false,
    use_xft = true,
    font = 'monospace:size=6',
    gap_x = 20,
    gap_y = 4,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_colour = '#08090c',
    update_interval = 10,
    uppercase = false,
}

conky.text = [[
${color}/////// \
${color1}${exec date "+%H:%M"} \
${color}///////// \
${color1}${memfree} \
${color}/////////// \
${color1}${fs_free /home} \
${color}///////////// \
${color1}${if_match "${exec ifconfig iwm0 | grep status | awk '{print $2}'}" == "active"}\
${exec ifconfig iwm0 | grep ieee80211 | awk '{print $3}'}\
${else}N/A${endif} \
${color}///////////////
${voffset 3}${alignr}/////// \
${color1}${if_match ${exec sndioctl -n output.mute} > 0}OFF\
${else}${exec sndioctl -n output.level | awk '{print int($0*100)}'}%${endif} \
${color}///////// \
${color1}${exec xbacklight -get | cut -d . -f 1}% \
${color}/////////// \
${color1}${exec apm -l}% \
${color}///////////// \
${color1}${exec sysctl -n hw.sensors.cpu0.temp0 | cut -d '.' -f 1}°C \
${color}///////////////   \
]];
