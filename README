puffy
-----

Dotfiles for OpenBSD.

doas

    $ echo "permit persist USERNAME" > /etc/doas.conf

apmd

    $ rcctl enable apmd
    $ rcctl set apmd flags -A
    $ rcctl start apmd

xenodm

    $ rcctl enable xenodm
    $ rcctl start xenodm
